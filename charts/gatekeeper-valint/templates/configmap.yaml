apiVersion: v1
kind: ConfigMap
metadata:
  name: gatekeeper-valint-config
  namespace: '{{ .Release.namespace }}'
  labels:
  {{- include "gatekeeper-valint.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ .Values.valint.config | toYaml | indent 4 }}
