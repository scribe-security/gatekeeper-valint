apiVersion: v1
kind: Secret
metadata:
  name: scribe-cred-secret
stringData:
  namespace: {{ .Release.Namespace }}
  scribe_client_id: {{ .Values.scribe_client_id }}
  scribe_client_secret: {{ .Values.scribe_client_secret }}
  scribe_enable: {{ .Values.scribe_enable }}

