attest:
  default: ""
  cocosign:
    signer:
      x509:
        enable: false
        private: testdata/keys/test.key.pem
        cert: testdata/keys/test.cert.pem
        ca: testdata/keys/ca-chain.cert.pem
    verifier:
      x509:
        enable: false
        cert: testdata/keys/test.cert.pem
        ca: testdata/keys/ca-chain.cert.pem