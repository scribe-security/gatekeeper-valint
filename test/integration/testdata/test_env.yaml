attest:
  # default: ""
  cocosign:
    signer:
      x509:
        enable: false
        private: env://test_key
        cert: env://test_cert
        ca: env://test_ca
    verifier:
      x509:
        enable: false
        cert: env://test_cert
        ca: env://test_ca